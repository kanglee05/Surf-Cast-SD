# `app/` — Plotly Dash (Week 5)

Week 1: no UI here yet.

**Project default:** Plotly Dash + Plotly charts (see project overview doc).

When you start Week 5, add `app.py` (or `dash_app/`) and point deployment (Render/Railway) at the Dash server entrypoint.
