# SurfCast SD — Week 1 baseline (pin versions when you lock the env)
pandas>=2.0,<3
numpy>=1.26,<3
requests>=2.31,<3
python-dateutil>=2.8,<3
tzdata>=2024.1

# Dev / tests (optional Week 1)
pytest>=8.0,<9
