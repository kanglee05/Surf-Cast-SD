# `models/` — saved artifacts (Week 3+)

Store `joblib` or pickle exports here **locally** — do not commit large binaries. Document the training commit hash and `requirements.txt` pin in your PR when you add a model.
