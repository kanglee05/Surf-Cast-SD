services:
  - type: web
    name: surfcast-sd
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app.app:server
    envVars:
      - key: PORT
        value: 10000
