# `data/processed/` — derived tables (Week 2+)

Week 1 output can be written here manually, e.g.:

```bash
python scripts/run_week1.py --days 30 --out data/processed/week1_merged.csv
```

Parquet requires `pyarrow` (`pip install pyarrow`).
