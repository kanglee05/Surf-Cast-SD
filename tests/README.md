# Tests

Add tests as you implement ETL and merge — start with **small fake DataFrames** and **no network**.

Suggested first test: call ``merge_hourly`` on two tiny hand-built frames and assert column names / row count.
